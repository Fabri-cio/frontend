const BotonActualizar = ({ onClick }) => {
  return (
    <button onClick={onClick} className="btn-actualizar">
      Actualizar
    </button>
  );
};

export default BotonActualizar; // Exporta el componente
